# FExperience — Redesign Specification v4 (Final)
## "Quiet Premium" — Light Editorial + Apple Glass

Status: Ready for development review
Основано на: FExperience Master Specification v2.0/2.1, мудборде "Spatial Premium", полном аудите живого сайта (`/`, `/about`, `/articles`, `/expeditions`, `/expeditions/south-africa`, `/expeditions/brazil`), 4 визуальных референсах.

Это единственный актуальный документ — предыдущие версии (v3.0, v3.1) объединены в него и больше не нужны как отдельные файлы.

---

## 0. Суть решения

Фон светлый, оранжевый — фирменный цвет (не декоративный акцент), стекло — точечное, Apple-style. Ориентиры: Financial Times, Monocle, Kinfolk, Apple Editorial, Forbes print. Не Bloomberg-терминал, не tech-startup bento (Linear/Vercel), не туристический luxury travel.

Один принцип держит всю систему вместе: **1–2 по-настоящему проработанных "wow"-момента на странице, всё остальное — тихое.** Не равномерно наляпанное стекло и градиенты везде, а осознанная редкость: одна featured-карточка, один hero-приём, одна карта.

---

## 1. Продуктовое позиционирование

FExperience — деловая платформа Forbes, дающая предпринимателям доступ к людям, рынкам и возможностям через кураторские бизнес-экспедиции. Не туристический продукт, не luxury travel — визуально и текстово это продолжение делового медиа Forbes.

На сайте уже есть точный манифест (`/about`), переносим почти дословно:

> «Мы помогаем предпринимателям открывать новые горизонты на самых перспективных рынках — в России и за рубежом. Мы не организуем туристические поездки. Мы проводим бизнес-экспедиции. Результат — ваше взвешенное стратегическое решение об экспансии бизнеса.»

**Модель опыта:**
```
Market → Insight → Expert Access → Expedition → Decision
```

Поправка: сайт сегодня успешно продаёт через конкретные горящие экспедиции (даты, счётчик мест) — отказываться от этого не нужно. Платформенное позиционирование — в заголовке и структуре hero; конкретная ближайшая экспедиция — вспомогательный слой под ним, не единственный смысл первого экрана.

---

## 2. Информационная архитектура

### 2.1 Навигация

```
Логотип   |   Рынки   Экспедиции   Статьи   О нас   |   [Стать участником]
```

Названия "Стать участником", "О нас", "Статьи" — сохраняются, они уже так подписаны на сайте. Добавляется пункт **"Рынки"** — единственное реальное изменение навигации, открывает Continental Navigation (Африка / Азия / LATAM / Россия) прежде, чем пользователь попадёт в конкретную страну. "Почему FExperience" и "Отзывы" остаются якорными блоками на главной, не пунктами меню — так уже правильно устроено.

### 2.2 Иерархия сущностей

```
Уровень 1 — Markets (регионы → страны)
Уровень 2 — Insights (статьи)
Уровень 3 — Experts (только внутри страницы экспедиции)
Уровень 4 — Expeditions (конкретная поездка)
Уровень 5 — Application (заявка)
```

---

## 3. Brand Experience Principles

- **Discover Before Others** — эмоция "я получаю доступ к рынку раньше других", не "я покупаю поездку".
- **Premium without Luxury** — премиальность через пространство, типографику, ритм и контент, не золото/мрамор/статусность.
- **Information over Decoration** — при выборе "эффектнее или понятнее" — всегда понятнее.
- **Calm Motion** — пользователь не должен замечать анимацию, должен замечать качество.
- **Photography** — только инфраструктура, деловая среда, предприниматели, индустрия. Никаких пляжей, отелей, самолётов, достопримечательностей. Фото на `/about` (деловая программа, 4 кадра) — эталон подбора на будущее.
- Ориентиры визуального языка: Financial Times, Monocle, Kinfolk, Apple Editorial, Forbes print — не Bloomberg-терминал и не Apple/Linear/Vercel tech-bento. Карточки должны читаться как **страницы делового досье**, не как SaaS-лендинг.

---

## 4. Дизайн-система

### 4.1 Цвет

```css
--bg-canvas: #FFF8F3;
--bg-surface: #FFF1E8;
--bg-elevated: rgba(255,255,255,.72);

--text-primary: #1A1A1A;
--text-secondary: #6D6D6D;
--text-tertiary: #A0A0A0;

--orange-700: #B7410E;   /* текст/ссылки/цифры на светлом — лучший контраст */
--orange-600: #FF6B2C;   /* базовый бренд-оранжевый — CTA, теги, активная навигация */
--orange-500: #FF7722;   /* вторичный акцент */
--orange-400: #FF8A54;   /* градиенты, hover */
--orange-300: #FFB089;
--orange-200: #FFE8D6;   /* фон тегов/плашек */
--orange-100: #FFF0E8;   /* самый светлый — фон секций-акцентов */

--gradient-signature: linear-gradient(45deg, #B7410E 0%, #FF7722 55%, #FFE8D6 100%);
--gradient-editorial-warm: linear-gradient(135deg, #FFF8F3 0%, #FFE8D6 48%, #FFF0E8 100%);
--continent-outline: #FFE6D8;
```

**Правило дистрибуции:** оранжевый — 25–35% визуального языка, часть бренда, не редкий акцент. Используется в CTA, активной навигации, тегах, крупных цифрах статистики, pull quotes, маршрутных линиях, разделителях. Насыщенным оранжевым запрещено заливать полноэкранные секции. При этом `--gradient-editorial-warm` разрешён как полноценный мягкий фон секции: он должен оставаться светлым и тёплым, без неоновой насыщенности.

### 4.2 Типографика

```
Заголовки (serif):  Playfair Display
Текст (sans):        HelveticaNeueCyr
Подключение: оба локально через `next/font/local` (`localFont`)
```

Крупные цифры статистики (">60%", "50+" — уже на сайте) — Playfair Display, `--orange-600`. Сильный приём, сохранить и унифицировать по всему сайту.

### 4.3 Стекло — три уровня

**Level 01 — базовое** (карточки, панели):
```css
background: rgba(255,255,255,.55);
backdrop-filter: blur(20px);
border: 1px solid rgba(255,255,255,.85);
box-shadow: 0 8px 40px rgba(0,0,0,.06);
```

**Level 02 — приподнятое** (hover, активные карточки):
```css
background: rgba(255,255,255,.72);
backdrop-filter: blur(24px);
```

**Glass Signature — редкий, третий уровень** (референс: карточка "Launch Your Ambitions" со светящимся контуром по углам):
```css
background: rgba(255, 250, 244, .6);
backdrop-filter: blur(18px);
--glow-color: rgba(255,107,44,.35);
box-shadow: 0 0 24px var(--glow-color), 0 12px 48px rgba(0,0,0,.08);
/* контур — короткие SVG-дуги 2–3px в --orange-500 по 2 углам, не сплошная рамка */
```
Только для: featured-карточки медиа-раздела, обложка featured-статьи в Insights, hero-плашка ближайшей экспедиции. Не больше 1 карточки на экран.

**Liquid Glass — самый редкий элемент** (капля янтаря, а не UI-компонент):
```css
background: linear-gradient(135deg, #FF8A54, #FF6B2C);
box-shadow: 0 0 40px rgba(255,107,44,.25);
transform: scale(1.03); /* hover */
```
Только для: `Стать участником` / `Подать заявку` / `Подписаться на рассылку`. Вторичные кнопки ("Подробнее") — плоская контурная пилюля. Как только это становится общим паттерном — эффект теряется.

Референсы стекла: Apple Vision OS, Arc Browser, Linear, Raycast — только механика стекла, не общая эстетика.

**Apple-like glass rule для контентных секций:** стеклянная поверхность должна быть прозрачной настолько, чтобы под ней читалось изображение/градиент. Обязательны blur + saturation + тонкий светлый edge + внутренний верхний highlight. Не использовать непрозрачный белый прямоугольник с blur как замену стеклу. Оранжевый glow — только для редких Signature-элементов.

### 4.4 Карточки и Bento

**Bento не является базовым паттерном сайта.** Если сетка используется, она должна быть оправдана контентом и иметь editorial-ритм. Приоритет: variable-height composition, asymmetrical spread, editorial rows и image-led layouts.

Карточка должна читаться как часть композиции, а не как автономный SaaS-модуль. Одинаковые 3/4/6 карточек подряд запрещены. Glass — материал отдельных поверхностей, а не обязательный фон каждой карточки.

```
Card radius: только там, где форма действительно нужна; не округлять весь контент одинаково.
Card padding: определяется композицией, а не одним глобальным значением.
Hover: image zoom / line reveal / border accent; массовый translateY запрещён.
Featured element: может использовать --gradient-signature, но тёплый editorial gradient является более частым фоном.
```

### 4.5 Карта — компонент "Свой континент" — НЕ УДАЛЯТЬ

**Критический lock для страниц экспедиций:** это не декоративный элемент и не альтернатива Continental Navigation. `MapContinent` — отдельный route-specific компонент детальной страницы `/expeditions/[slug]`, который показывает конкретный континент/регион, активную страну и при необходимости маршрут программы.

На Homepage и `/expeditions` работает `Continental Navigation`; на детальной странице работает `MapContinent`. Эти компоненты нельзя объединять или заменять один другим.

Заменяет единый `world-map.svg`, который сейчас одинаково стоит на всех страницах. Новый компонент **привязан к конкретной экспедиции**: на вход — код региона + координата активной точки, на выходе — свой контур для каждой страницы.

```
Контур континента — 1.5px, --continent-outline, увеличен так, чтобы активная страна была в визуальном центре
Соседние точки (другие страны того же континента, без ссылок, для масштаба) — контурный круг 6px, --text-tertiary, opacity .5
Активная точка — залитый круг 12px, --orange-600, подписана, pulse (opacity 0.4→0, 2s loop) — кроме статуса "Завершена" (см. 6.4)
Плашка-подпись — Glass Level 01, заголовок Playfair 20–24px
```

```
/expeditions/south-africa → контур Африки, точка на ЮАР
/expeditions/vietnam      → контур Азии, точка на Вьетнаме
/expeditions/india        → контур Азии, точка на Индии
/expeditions/brazil       → контур LATAM, точка на Бразилии
/expeditions/kenya        → контур Африки, точка на Кении
/expeditions/morocco      → контур Африки, точка на Марокко (без pulse)
/expeditions/sakhalin     → контур России/Дальнего Востока (вне 4 цветов регионов)
/expeditions/thailand,
/expeditions/indonesia    → контур Азии, соответствующая точка
```

На директории (`/expeditions`) общей карты-обзора больше нет — эту роль выполняет Continental Navigation. Своя карта появляется только на детальной странице конкретной экспедиции.

### 4.6 Motion

Спокойные короткие анимации (300–500ms, ease-out). Карта "дорисовывается" при скролле; editorial rows программы могут проявляться последовательно — разрешено, если это поддерживает сторителлинг. Не использовать массовый fade-up для карточек.

Заголовок hero — разовая анимация появления при загрузке (fade/slide). **Живая подмена текста в зависимости от времени суток исключена из ТЗ** — противоречит Calm Motion и Information over Decoration, риск восприниматься как гаджет ради гаджета для аудитории, решающей вопрос выхода на рынок за $50k+. Может остаться опциональным A/B-экспериментом, не обязательной фичей релиза.

---

## 5. Continental Navigation System

4 региона вместо карты мира, editorial market index без обязательных glass-карточек. Контуры регионов и hairline-разделители являются частью навигационной композиции; Glass Level 01 допускается только как отдельное active/focused state, а не как фон всех четырёх элементов.

```css
--continent-outline: #FFE6D8;
--market-dot-active: #FF6B2C;
```

| Регион | Температура | Направления сайта |
|---|---|---|
| Africa | `#FF6B2C` | ЮАР (активна), Марокко (завершена), Кения (скоро) |
| Asia | `#FF8A54` | Вьетнам (активна), Индия (активна), Таиланд (скоро), Индонезия (скоро) |
| LATAM | `#FF9E6E` | Бразилия (скоро) |
| Russia / Domestic | `#FFB089` | Сахалин (скоро) — отдельный нецветной блок, вне континентальной палитры |

Присутствует на: Homepage, Expeditions Directory, Footer, Mobile Navigation.

---

## 6. Постранично

Общий шаблон: `Header → Page Hero → Content Sections → CTA Layer → Footer`.

### 6.1 Homepage

**Header** — светлый, липкий, Glass Level 01 при скролле.

**Hero — сплит-стекло.** Единый fullbleed видеофон (деловая среда, не туризм), поверх него — крупный блок frosted glass, сквозь который частично проступает decorative wordmark (15–20% непрозрачности), поверх стекла — читаемый заголовок платформенного уровня и CTA полной контрастности. Не "текст слева / фото справа" — приём почти у всех травел-сайтов, а текст, прорастающий сквозь стекло.

Под hero — тонкая плашка (не блюр, светлая пилюля с рамкой) с ближайшей активной экспедицией: ротируется между ЮАР (15–21 ноября 2026), Вьетнам (14–20 марта 2027), Индия (сентябрь 2026) — по ближайшему дедлайну/меньшему числу мест.

**Market Reality (editorial data field)** — три текущие цифры сайта (">60% неудачных экспансий", "50+ лояльных контактов", ">40% стартапов из-за невостребованности продукта") — не отдельные карточки, а единая асимметричная композиция на мягком светлом оранжевом градиенте `--gradient-editorial-warm`. Крупная serif-типографика, тонкие разделители, первый показатель визуально доминирует.

**Platform Statement (editorial statement)** — блок "FExperience — специальный проект команды Forbes..." переносится по смыслу без изменения контента, но перестраивается из центрированного SaaS-подобного блока в асимметричную editorial-композицию: statement слева, три принципа справа в виде нумерованного вертикального списка без карточек и pills.

**Continental Navigation** — editorial market index: крупные регионы, номера, контуры, hairline-разделители; не четыре одинаковые glass-карточки.

**Featured Markets** — editorial market spread, не bento-шаблон. Разные визуальные веса, большие изображения и типографика.

**How FExperience Works** — image-led compositions + отдельные Apple-like glass overlays; не шесть одинаковых карточек.

**Media Layer** — data-led editorial block, крупные цифры и изображения Forbes.

**Testimonials** — один крупный отзыв за раз с одной glass-панелью, а не ряд одинаковых Glass-карточек.

**FAQ** — тихий accordion без card containers.

**Final CTA** — тёплая editorial gradient-плоскость + один Liquid Glass CTA.

**Footer** — см. 6.7.

### 6.2 О нас

Реальная структура страницы, задача редизайна — чисто визуальная, контент не меняется:

1. **Манифест** — крупный serif по центру, тонкая оранжевая линия под "МЫ ПРОВОДИМ БИЗНЕС-ЭКСПЕДИЦИИ".
2. **"Деловая программа и погружение в локальную культуру"** — editorial photo field: одно доминирующее фото + три вторичных кадра разного масштаба, без равномерной bento-сетки.
3. **Due Diligence** — 9-пунктовое вертикальное досье с номерами 01/09 … 09/09 и hairline-разделителями, без 9 одинаковых карточек.
4. **Команда проекта** — editorial roster с крупным портретом и типографическим списком; Forbes Russia badge — функциональный маркер, не декоративная pill.

### 6.3 Экспедиции — Directory

Статусы:
```
Активна   — ЮАР (15–21 ноября 2026), Вьетнам (14–20 марта 2027), Индия (сентябрь 2026, Нью-Дели)
Скоро     — Бразилия, Сахалин, Таиланд, Индонезия, Кения
Завершена — Марокко (10 спикеров)
```

Статус — небольшой функциональный бейдж/пилюля вверху editorial-item, не главный визуальный элемент: `Активна` — заливка `--orange-100`, текст `--orange-600`; `Скоро` — контурная пилюля `--text-tertiary`; `Завершена` — приглушённая, `--bg-surface`, `--text-secondary`, карточка визуально "тише" (без hover-подъёма). Считывается за долю секунды без чтения текста.

Структура страницы: Hero → Continental Navigation → Featured Markets → Industry Explorer → editorial directory variable-height → Upcoming Markets.

### 6.4 Детальная страница экспедиции — три шаблона по статусу

Шаблон зависит от статуса карточки, не от произвольного "состояния". У экспедиций "Скоро" нет ни программы, ни экспертов — это ожидаемая структура, не пробел в контенте.

**Шаблон 1 — Активна (ЮАР, Вьетнам, Индия). Полная программа.**
- Hero: даты, заголовок "Бизнес-экспедиция в [страна] с Forbes", 3 стеклянные пилюли-тега (погружение в культуру / эксклюзивный нетворкинг / лучшие бизнес-практики).
- **Карта "свой континент" сразу под hero — обязательна.** Она остаётся отдельным визуальным центром страницы и реализуется через `MapContinent`; не заменять её карточкой, глобальной картой мира или маленьким декоративным SVG.
- "Что включено" — 6 image-led compositions с плавающими Apple-like glass labels/panels. Не шесть одинаковых карточек.
- "Программа экспедиции" — по ЮАР реально 6 дней с named-локациями (Lanzerac Wine Estate, бизнес-сессия "Африка в фокусе", Стелленбосский университет, локальное предприятие + Кейптаун, Babylonstoren + "Говорят местные", мыс Доброй Надежды). Вертикальный editorial timeline: номер дня, крупный serif-заголовок, тонкая линия и фото; boxed cards и массовый fade-up не обязательны.
- "Наши эксперты" — по ЮАР 12 участников в editorial roster: 9 внешних экспертов + 3 представителя Forbes Russia. Фото и роль видны сразу; не использовать сетку одинаковых profile cards.
- Форма заявки — выпадающий список с выбором экспедиции (Индия / ЮАР / Вьетнам) + имя, телефон, email, согласие.

**Шаблон 2 — Скоро (Бразилия, Сахалин, Таиланд, Индонезия, Кения). Компактный.**
Без секций "Программа" и "Наши эксперты" — их отсутствие в разметке, не пустые заглушки.
- Hero с теми же тремя тегами + короткий питч под рынок (у Бразилии — "Латинская Америка: агросектор, энергетика, финансы").
- Карта "свой континент" — единственный визуальный контент страницы, кроме hero.
- Текст-приглашение + lead-форма: «Вам интересна локация? Оставьте заявку — и эта точка может стать следующей на карте FExperience» (реальный текст с сайта, сохраняется).
- "Похожие экспедиции" — 2–3 карточки активных направлений того же региона.

**Шаблон 3 — Завершена (Марокко). Recap.** *(предложение, требует вашего подтверждения перед реализацией — раньше такого шаблона на сайте не было)*
Логика — "витрина результата", конвертирует в подписку, а не в заявку на прошедшую поездку.
- Hero без счётчика мест и дат заезда — "Экспедиция состоялась" + агрегированная цифра ("10 спикеров").
- Карта "свой континент", точка на Марокко, без pulse-анимации.
- Отзывы участников именно этой экспедиции (Апалькова, Адамович, Цыганов — уже помечены "Марокко 2025" в карусели на главной), закреплены на странице постоянно.
- Ссылки на статьи по направлению (9 из 13 статей на `/articles` — про Марокко/Африку).
- CTA — "Подписаться на анонсы" или "Смотреть активные экспедиции", не "Стать участником".

Если шаблон 3 избыточен для первого этапа — Марокко может временно использовать упрощённую версию шаблона 2 (без lead-формы, с отзывами вместо приглашения).

### 6.5 Статьи / Insights Hub

Позиционирование: Premium Editorial Magazine, не база знаний. Реальная таксономия: категории **марокко, африка, прочее** (13 статей). Крупные обложки без рамок. Лента — variable-width magazine layout, не 3 одинаковые карточки. Hover — лёгкий зум + оранжевая линия под заголовком. Фильтры — текстовые tabs с активной линией, а не ряд одинаковых pills. Featured Insight — большой editorial spread.

### 6.6 Шаблон статьи

- Hero: крупный заголовок serif, excerpt, категория-тег `--orange-200`/`--orange-600`.
- Метаданные: автор, дата, время чтения.
- Тело: HelveticaNeueCyr, крупный line-height.
- Pull quotes: `--orange-700` (не 600 — контраст в длинном тексте), тонкая акцентная линия.
- Inline data cards — врезки со статистикой рынка.
- Related Market / Related Insights — обязательный блок в конце.
- Subscribe CTA.

### 6.7 Футер

На сайте уже светлый минималистичный блок (контакты, 3 ссылки навигации, юр. адрес, копирайт) — не тёмный, как предполагалось в старой версии спецификации. Добавляется:

```css
background: #F0ECE5;
border-top: 1px solid rgba(255,107,44,.15);
```

Структура: newsletter-форма (текст "Узнайте первыми о предстоящих мероприятиях" уже есть на сайте как анонс — превратить в реальную рабочую форму) → Continental Navigation footer → 4 колонки навигации → platform statement → правовой блок. Ссылки `--text-secondary`, hover `--orange-600`.

---

## 7. Анти-шаблонность — обязательный visual lock

1. Hero сквозь стекло (не "текст слева / фото справа").
2. Glass Signature с угловыми дугами-неонками, не сплошной рамкой.
3. Статус-бейджи вместо статус-текста.
4. Позиция эксперта видна сразу, не по hover.
5. Featured-карточка на фирменном градиенте — единственный цветной "остров" в светлой сетке.
6. Due Diligence как страницы досье (нумерация 01/09…09/09), не SaaS-иконки в кружках.
7. Карта с pulse-анимацией только на активной точке.
8. После Hero запрещён повторяющийся паттерн `heading → 3/4/6 одинаковых cards → shadow → pill → CTA`.
9. Не использовать белый фон как автоматический default каждой секции. Основной canvas — тёплый; `--gradient-editorial-warm` может быть полноценной плоскостью между секциями.
10. Glass — материал композиции, а не тип карточки: Hero, отдельные панели `Что включено`, отдельные testimonial/featured моменты.
11. Для каждой крупной секции выбирать один из разных композиционных типов: editorial field, image-led spread, dossier list, market index, glass overlay, quiet information rows.
12. Bento разрешён только если он уникально решает композиционную задачу; механическое `2×2 / 3×3 card grid` запрещено.
13. Pills и rounded containers не использовать автоматически.
14. Hover не должен массово поднимать карточки. Предпочтительны image zoom, line reveal, border/accent changes и subtle glass refraction.
15. Визуальный уровень Hero должен продолжаться не количеством эффектов, а качеством композиции: крупная типографика, асимметрия, материал изображения, воздух и редкое стекло.
16. Карта `MapContinent` на детальных страницах экспедиций — обязательный индивидуальный элемент FExperience. Не превращать её в generic map widget и не смешивать с Continental Navigation.
17. Если новый компонент можно заменить стандартной UI-карточкой без потери смысла, сначала проверить, не является ли это возвратом к AI-шаблону; по умолчанию предпочитать editorial composition.

---

## 8. Технические токены и стек

```
Frontend: Next.js 15+
Язык: TypeScript
Стили: Tailwind CSS
Анимация: Framer Motion
CMS: отсутствует. Все данные проекта захардкожены в `src/data/`.
Хостинг: Docker / standalone deployment
```

```css
:root {
  --bg-canvas: #FFF8F3;
  --bg-surface: #FFF1E8;
  --bg-elevated: rgba(255,255,255,.72);

  --text-primary: #1A1A1A;
  --text-secondary: #6D6D6D;
  --text-tertiary: #A0A0A0;

  --orange-700: #B7410E;
  --orange-600: #FF6B2C;
  --orange-500: #FF7722;
  --orange-400: #FF8A54;
  --orange-300: #FFB089;
  --orange-200: #FFE8D6;
  --orange-100: #FFF0E8;

  --gradient-signature: linear-gradient(45deg, #B7410E 0%, #FF7722 55%, #FFE8D6 100%);
--gradient-editorial-warm: linear-gradient(135deg, #FFF8F3 0%, #FFE8D6 48%, #FFF0E8 100%);
  --gradient-editorial-warm: linear-gradient(135deg, #FFF0E8 0%, #FFE8D6 48%, #FFF8F3 100%);
  --continent-outline: #FFE6D8;

  --radius-sm: 8px;
  --radius-md: 16px;
  --radius-lg: 24px;

  --space-xs: 8px;
  --space-s: 16px;
  --space-m: 24px;
  --space-l: 40px;
  --space-xl: 64px;

  --duration-fast: 150ms;
  --duration-normal: 300ms;
  --duration-slow: 500ms;
}
```

---

## 9. Чек-лист перед разработкой

- [x] Контент `/about`, `/articles`, `/expeditions`, `/expeditions/south-africa`, `/expeditions/brazil` — снят и учтён.
- [ ] Снять содержимое остальных детальных страниц (Вьетнам, Марокко, Сахалин, Индия, Таиланд, Индонезия, Кения) — вероятно, шаблон 1 или 2, перепроверить у каждой отдельно.
- [ ] Все фото/видео — сверить с принципом "деловая среда, не туризм".
- [ ] Единая оранжевая шкала выше — использовать только эти значения.
- [ ] Glass Signature и Liquid Glass — не более 1–2 применений на экран каждое.
- [ ] "Рынки" в навигации — можно как редирект на Continental Navigation внутри `/expeditions`, не обязательно отдельный URL.
- [ ] Живая подмена заголовка по времени суток — исключить из обязательного релиза, максимум A/B-эксперимент.
- [ ] Шаблон "Завершена" (6.4, Шаблон 3) — подтвердить у заказчика перед реализацией, это новое предложение, не то, что было на сайте изначально.
- [ ] Форма подписки в футере — проверить, подключена ли она сейчас технически, на аудите видна только текст-анонс.

---

END OF DOCUMENT
